package com.example.looksoon.ui.theme

import androidx.compose.ui.graphics.Color

// Fondo y superficies
val Background = Color(0xFF000000)   // Fondo principal Negro
val Main = Color(0xFF2A003F)   // Fondo principal Morado oscuro
val Surface = Color(0xFF1C1C1C)      // Superficie de cards/containers
val Divider = Color(0xFF2E2E2E)      // Divisores y bordes sutiles

// Texto
val TextPrimary = Color(0xFFFFFFFF)  // Texto principal (blanco)
val TextSecondary = Color(0xFFB3B3B3) // Texto secundario / subtítulos
val TextDisabled = Color(0xFF666666) // Texto o íconos deshabilitados

// Colores de acento
val PurplePrimary = Color(0xFF9B4DFF) // Púrpura principal (tabs activas, badges)
val PurpleSecondary = Color(0xFFC77DFF) // Púrpura suave (hover, estados secundarios)
